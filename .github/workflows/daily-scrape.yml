name: Daily Schedule, Weather & Homeshopping Scrape

on:
  schedule:
    # 매일 한국시간(KST) 06:00 = UTC 21:00 (전날)
    - cron: "0 21 * * *"
  workflow_dispatch: {}  # 수동 실행 버튼

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v4

      - name: Python 설치
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 패키지 설치
        run: pip install requests beautifulsoup4

      - name: 편성표 스크래퍼 실행
        run: python naver_schedule_scraper.py

      - name: 날씨 스크래퍼 실행
        env:
          API_KEY: ${{ secrets.WEATHER_API_KEY }}
        run: python weather.py

      - name: 홈쇼핑 스크래퍼 실행 - 현대(HD)
        continue-on-error: true
        run: python hd_scraper.py

      - name: 홈쇼핑 스크래퍼 실행 - 롯데(LT)
        continue-on-error: true
        run: python lt_scraper.py

      - name: 홈쇼핑 스크래퍼 실행 - CJ온스타일(CJ)
        continue-on-error: true
        run: python cj_scraper.py

      - name: 홈쇼핑 스크래퍼 실행 - GS(라방바 경유)
        continue-on-error: true
        run: python gs_scraper.py

      - name: 결과 커밋 & 푸시
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/*.json weather/asos/*.json weather/forecast/*.json homeshopping/**/*.json
          git diff --staged --quiet || git commit -m "chore: 편성표·날씨·홈쇼핑 자동 갱신 $(date -u +%Y-%m-%d)"
          git push
